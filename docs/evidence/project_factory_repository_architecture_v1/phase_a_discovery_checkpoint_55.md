# Phase A Discovery Checkpoint 55 — P5 adaptive routing ready for focused validation

Date: 2026-09-21

P4 is closed at accepted P4C.

P5 begins on a separate branch from the exact accepted P4C source.

## Source

Branch:

`project-factory-token-efficiency-v4-p5`

Base:

`0cff265e5b92dc73c8ca1526da8664126d06af1b`

Current SHA:

`533ae4e4f03f6faf10b5b316ada7edcabb49972d`

Canonical `main` remains untouched.

## P5 changes

Company OS:

- adds deterministic economy-candidate metadata to execution strategy;
- advances resource-strategy artifact version to 2;
- keeps primary model recommendation limited to standard/strongest.

Runner:

- knows an abstract economy target mapped to `haiku` by default;
- adds `--economy-model`;
- downshifts developers only after immutable-base diagnostic evidence;
- verifies counted failures match extracted hints;
- verifies every failure hint has a complete failure-guided AST span;
- preserves operator-pinned models;
- never downshifts strongest-tier work;
- persists applied/veto evidence.

## Tests added

Static tests cover:

- narrow first-attempt low-risk candidate eligibility;
- medium risk, multi-path, no-test, correction, novel, specialist and review ineligibility;
- strongest-tier ineligibility;
- green base test veto;
- successful fully localized developer downshift;
- reviewer remains standard;
- forged strongest candidate cannot downshift;
- primary briefing model tier `economy` is refused.

## Decision

Run zero-provider focused validation first.

No paid model benchmark is authorized yet.

No canonical merge, deployment, publishing or production integration is authorized.
